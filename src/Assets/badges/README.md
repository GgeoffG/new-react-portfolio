Drop certification badge images here (png/svg/jpg).

To show official badge art instead of the text placeholder for a
certification: download the badge image from the issuer (e.g. Credly's
"Share -> Download badge image"), save it in this folder, and set the
matching filename in the `image` field for that cert in
`src/components/particles/resumeData.js`.
